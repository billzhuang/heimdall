# Anthropic API Key (required)
ANTHROPIC_API_KEY=your-api-key-here


# Kubernetes configuration (optional - defaults shown)
KUBECONFIG=~/.kube/config
# K8S_CONTEXT=your-context-name

# Default cluster name (optional - can be passed via CLI)
# HEIMDALL_CLUSTER=your-cluster-name
